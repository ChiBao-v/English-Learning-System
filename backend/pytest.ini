[pytest]
DJANGO_SETTINGS_MODULE = toeic_project.settings
python_files = tests.py test_*.py *_tests.py
addopts = -q --reuse-db
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
